// Sub-project standalone — peut être ouvert seul dans IntelliJ pour valider
// que le code Java compile sans erreur rouge dans l'éditeur (sinon PSI
// échouera silencieusement).
rootProject.name = "test-project"
